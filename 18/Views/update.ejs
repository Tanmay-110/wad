<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Update Song</title>
  </head>
  <body>
    <h1>Update Actor and Actress</h1>
    <form
      action="/update/<%= encodeURIComponent(song.songname) %>"
      method="POST"
    >
      <p>Song Name: <strong><%= song.songname %></strong></p>
      <p>Film: <%= song.film %></p>
      <label>Actor:</label>
      <input
        type="text"
        name="actor"
        value="<%= song.actor || '' %>"
        required
      /><br />
      <label>Actress:</label>
      <input
        type="text"
        name="actress"
        value="<%= song.actress || '' %>"
        required
      /><br /><br />
      <button type="submit">Update</button>
    </form>
    <br />
    <a href="/">Back to Song List</a>
  </body>
</html>
